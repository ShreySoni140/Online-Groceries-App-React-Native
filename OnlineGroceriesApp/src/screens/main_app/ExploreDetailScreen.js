import React from "react";
import { StyleSheet, Text } from "react-native";

const ExploreDetailScreen = (props) => {
  return <Text>ExploreDetailScreen</Text>;
};

const styles = StyleSheet.create({});

export default ExploreDetailScreen;
